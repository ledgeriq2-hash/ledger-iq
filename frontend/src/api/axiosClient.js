import axiosClient, {
  ACCESS_TOKEN_STORAGE_KEY,
  API_BASE_URL,
  setErrorNotifier,
  TENANT_NOT_SET_ERROR,
} from "./index";

export { ACCESS_TOKEN_STORAGE_KEY, API_BASE_URL, setErrorNotifier, TENANT_NOT_SET_ERROR };
export default axiosClient;

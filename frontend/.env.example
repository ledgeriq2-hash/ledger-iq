# (Optional) override the backend origin (e.g. http://localhost:8000). When omitted, the browser uses the current host.
VITE_API_ORIGIN=

# (Optional) append a base path (defaults to /api).
VITE_API_BASE=/api

VITE_TENANT_ID=
VITE_ACTOR_ID=
